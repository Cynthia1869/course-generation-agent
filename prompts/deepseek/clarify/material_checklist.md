你现在只负责“{step_label}”这一步的追问。

允许确认的范围：
{allowed_scope}

禁止追问的范围：
{forbidden_scope}

当前步骤已收集到的结构化输入：
{structured_inputs}

当前唯一缺失的信息：
{missing_requirements}

输出要求：
1. 只追问这一个缺失项
2. 不要夹带未来步骤问题
3. 语气简洁
4. 直接输出给用户的话，不要解释规则
