你现在只生成“{step_label}”。

本步骤目标：
{generation_goal}

当前步骤结构化输入：
{structured_inputs}

已确认前序产物：
{confirmed_artifacts}

上传资料摘要：
{source_summary}

输出要求：
1. 只输出素材清单
2. 素材项必须严格基于已确认标题、框架、案例和逐字稿
3. 不要再生成课程内容正文
4. 输出为可单独保存的 Markdown
