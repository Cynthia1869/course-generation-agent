你现在只生成“{step_label}”。

本步骤目标：
{generation_goal}

当前步骤结构化输入：
{structured_inputs}

已确认前序产物：
{confirmed_artifacts}

上传资料摘要：
{source_summary}

输出要求：
1. 只输出逐字稿相关内容，不要生成课程标题、课程框架、案例、素材清单
2. 基于当前已确认信息，生成可直接用于讲解和录制的逐字稿
3. 逐字稿必须严格围绕已确认的课程框架和案例展开，不要擅自新增未确认的内容
4. 每个教学环节开头都要先说明这一部分要解决什么问题，再进入讲解
5. 每个模块结束后都要写出承上启下的衔接内容，说明为什么要进入下一部分
6. 讲解必须紧贴案例操作过程展开，把知识点嵌在操作里讲，不要脱离案例空讲理论
7. 语言要自然、清楚、像真实讲师在带学员操作，避免空话、套话、比喻和过度营销表达
8. 如果需要切换演示、等待学员操作或提示观察结果，可以使用括号标注，例如（切换屏幕演示）（等待学员操作）
9. 如果课程涉及 AI 工具或大模型，要在合适的位置自然说明：同样输入可能得到不同结果，以及遇到结果不一致时该如何判断和调整
10. 逐字稿要兼顾可讲性和可录制性，句子不要过于书面，也不要碎得太厉害
11. 不要编造未确认的信息，如果信息不足，就基于已有信息做最稳妥、最清晰的表达
12. 只输出可直接保存的 Markdown，不要解释规则，不要输出分析过程

输出格式：
## 课程开场
说明这节课讲什么、给谁学、要解决什么问题，以及学完后能得到什么结果。

## 模块一逐字稿
围绕模块一展开，先说明这一部分要解决的问题，再进入操作、现象、解释和小结。

## 模块一到模块二的衔接
说明模块一完成后，为什么要继续进入模块二，这里要自然过渡，不要生硬跳转。

## 模块二逐字稿
围绕模块二展开，先说明这一部分要解决的问题，再进入操作、现象、解释和小结。

## 模块二到模块三的衔接
说明模块二完成后，为什么要继续进入模块三，这里要自然过渡，不要生硬跳转。

## 模块三逐字稿
围绕模块三展开，先说明这一部分要解决的问题，再进入操作、现象、解释和小结。

如果课程框架中存在模块四或模块五，就继续按相同结构输出：
- 模块三到模块四的衔接
- 模块四逐字稿
- 模块四到模块五的衔接
- 模块五逐字稿

## 课程收束
总结本节课完成了什么、学员现在已经能做什么，以及后续如何承接到练习或素材使用。